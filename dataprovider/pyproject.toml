[project]
name = "dataprovider"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Victor Ionescu", email = "victor.ionescu.razvan@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.3",
    "fastapi[standard-no-fastapi-cloud-cli]>=0.141.1",
]

[project.scripts]
dataprovider = "dataprovider:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
